#!/usr/bin/env python3
"""
Mays Installer - Deployment Lifecycle CLI
Entry point for mays-installer command.
"""

import sys
import os

# Add project root to path
project_root = os.path.dirname(os.path.abspath(__file__))
if project_root not in sys.path:
    sys.path.insert(0, project_root)

from installer.cli.main import main

if __name__ == "__main__":
    sys.exit(main())